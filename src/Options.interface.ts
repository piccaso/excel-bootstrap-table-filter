/** Plugin Options */
interface Options {
    columnSelector: string,
    sort: boolean,
    search: boolean,
    autoUpdate: boolean,
    captions: {
        a_to_z: string,
        z_to_a: string,
        search: string,
        select_all: string
    }
}

